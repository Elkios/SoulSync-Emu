namespace SoulSync.Games
{
    /// <summary>Console family a game runs on (drives which emulator core/memory domain is used).</summary>
    public enum EmuSystem
    {
        NDS,
        GBA,
    }
}
